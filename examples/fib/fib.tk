fn printf(u8 *s, ...) i32;

fn fib(u32 n) u32 {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() i32 {
    u32 fib_of_42 = fib(42);
    printf("The 42nd Fibonacci number is %i.", fib(42));

    return 0;
}
